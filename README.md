# BionicDB

An API to download, and inspect Bionic libc versions used throughout Android Platforms. Comes along with a web frontend and MCP.

## Run in terminal

```sh
cd api && uv run uvicorn app.main:app
```

## Run with docker
```sh
docker-compose up --build
```
